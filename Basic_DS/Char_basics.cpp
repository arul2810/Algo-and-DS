#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main(){

    map <char, int> input_char;

    char temp;

    while(1){
        cin >> temp;
    if (temp != '\0'){

        input_char[temp]++;
    
    }else
    }

    cout << "TEst";
}